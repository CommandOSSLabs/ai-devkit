# SDL Phase 1 Scope

## Goal

Record and clarify architecture-level decisions early so implementation has stable guidance.

## ADR Fit

- Use ADRs for system-level decisions affecting multiple features or core architecture.
- Keep feature-local technical choices inside the feature spec unless they become system-wide.
